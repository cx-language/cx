// Nullable types with compile-time null-safety.
int? findAge(string name) {
    if name == "ada" return 36;
    return null;
}

void main() {
    int? age = findAge("ada");
    if age {
        println("ada is ", age);
    }
    println(findAge("grace")); // null
}
