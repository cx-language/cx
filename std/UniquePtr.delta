struct UniquePtr<Pointee> {
	Pointee* pointer;

	UniquePtr(Pointee value) {
		this.pointer = allocate(value);
	}

//	UniquePtr(Pointee* pointer) {
//		this.pointer = pointer;
//	}

	~UniquePtr() {
		deallocate(pointer);
	}

	Pointee* get() {
		return pointer;
	}
}
