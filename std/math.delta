T abs<T>(T value) {
    if (value < 0) {
        return -value;
    } else {
        return value;
    }
}

T clamp<T>(T value, T min, T max) {
    if (value < min) return min;
    if (value > max) return max;
    return value;
}
