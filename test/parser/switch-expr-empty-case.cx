// RUN: %not %cx -parse %s 2>&1 | %FileCheck %s

// CHECK: [[@LINE+4]]:9: error: switch expression case must have a value
int f(int n) {
    return switch n {
        case 0:
        default: 1
    };
}
