// RUN: %not %cx -parse %s 2>&1 | %FileCheck %s

// The old `test` specifier form is rejected; `@test` above the function replaces it.


// CHECK: [[@LINE+1]]:11: error: expected newline or ';', got identifier
test void f() {
}

