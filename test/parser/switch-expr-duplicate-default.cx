// RUN: %not %cx -parse %s 2>&1 | %FileCheck %s

// CHECK: [[@LINE+5]]:9: error: switch-expression may only contain one 'default' case
int f(int n) {
    return switch n {
        case 0: 1
        default: 2
        default: 3
    };
}
