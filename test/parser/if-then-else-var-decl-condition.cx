// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

void main() {
    int? c = 5;
    // CHECK: [[@LINE+1]]:12: error: variable declaration conditions are not supported in if expressions
    if var x = c then println(x) else println("none");
}
