// RUN: not %delta -typecheck %s | %FileCheck -strict-whitespace %s

def main() {
    // TODO: Should say 'expected type'.
    // CHECK: [[@LINE+1]]:15: error: expected identifier, 'mutable', or '(', got '>'
    var a = A<>();
}
