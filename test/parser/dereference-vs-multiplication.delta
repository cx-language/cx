// RUN: %delta -print-ast %s | %FileCheck %s

def f() {
    // CHECK: (var-stmt m (call g))
    // CHECK-NEXT: ((*a) = b)
    var m = g()
    *a = b

    // CHECK-NEXT: (var-stmt m ((call g) * a))
    var m = g()
    *a
}
