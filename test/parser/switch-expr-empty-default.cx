// RUN: %not %cx -parse %s 2>&1 | %FileCheck %s

// CHECK: [[@LINE+5]]:5: error: switch expression default case must have a value
int f(int n) {
    return switch n {
        case 0: 1
        default:
    };
}
