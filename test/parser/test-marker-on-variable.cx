// RUN: %not %cx -parse %s 2>&1 | %FileCheck %s

// CHECK-NOT: test-marker-on-variable.cx:{{[0-9]+}}:{{[0-9]+}}: {{(warning|error)}}:

// CHECK: [[@LINE+1]]:10: error: only functions can be marked as tests
test int x = 1;

// CHECK-NOT: test-marker-on-variable.cx:{{[0-9]+}}:{{[0-9]+}}: {{(warning|error)}}:
