global %x = int 0

int main() {
    int* %foo = alloca int
    store int 2 to %foo
    int %foo.load = load %foo
    bool %0 = %foo.load == int 1
    br %0, if.then, if.else

if.then:
    br %if.end

if.else:
    br %if.end

if.end:
    int %foo.load = load %foo
    bool %1 = %foo.load != int 0
    br %1, if.then, if.else

if.then:
    br %if.end

if.else:
    br %if.end

if.end:
    int %foo.load = load %foo
    switch %foo.load {
        int 0 -> switch.case.0
        int 1 -> switch.case.1
        int 2 -> switch.case.2
    }

switch.case.0:
    return int 0

switch.case.1:
    return int 0

switch.case.2:
    return int 0

switch.default:
    br %switch.end

switch.end:
    return int 0
}
