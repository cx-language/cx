
bool _EN4mainltEP1XP1X(X* %a, X* %b) {
    return bool true
}

void _EN4main2fxEP1XP1X(X* %a, X* %b) {
    X* %0 = cast %a to X*
    X* %1 = cast %b to X*
    bool %2 = call %_EN4mainltEP1XP1X(X* %0, X* %1)
    bool %3 = call %_EN4mainltEP1XP1X(X* %a, X* %b)
    return void
}

void _EN4main2fyEP1YP1Y(Y* %a, Y* %b) {
    uint64 %address = cast %a to uint64
    uint64 %address = cast %b to uint64
    bool %0 = %address < %address
    return void
}
