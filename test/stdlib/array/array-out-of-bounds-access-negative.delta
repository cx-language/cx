// RUN: check_exit_status 254 %delta run %s | %FileCheck %s

func main() -> int {
    var array = Array<int>();
    array.push(42);
    return *array[-1];
}

// CHECK: Array index -1 is out of bounds, size is 1
// CHECK: Abort
