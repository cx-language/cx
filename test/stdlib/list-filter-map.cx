// RUN: %cx test %s | %FileCheck %s

bool isEven(int& n) {
    return n % 2 == 0;
}

@test
void testFilterMap() {
    var numbers = List([0, 1, 2, 3, 4]);

    var even = numbers.filter(isEven).toList();
    // CHECK: [0, 2, 4]
    println(even);

    var doubled = even.map(n => n * 2).toList();
    // CHECK-NEXT: [0, 4, 8]
    // CHECK-NEXT: ok testFilterMap
    println(doubled);
}
