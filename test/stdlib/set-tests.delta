// RUN: check_exit_status 0 %delta run %s test-support.delta

func main() {
    var s = Set<StringRef>();

    s.insert("ok");
    s.insert("best");

    assert(s.contains("ok"), "ok");
    assert(!s.contains("lol"), "not lol");
}
