// RUN: check_exit_status 0 %cx test -Werror %s

struct A: Printable {
    string a;
    string b;

    void print(OutputStream& stream) {
        stream.append(a);
        stream.append("+");
        stream.append(b);
    }
}

@test
void testCustomPrintableStruct() {
    var text = A("foo", "bar").toString();
    assert(string(text) == "foo+bar");
}
