// RUN: check_exit_status 0 %cx run -Werror %s

void main() {
    int v = 5;
    int w = 6;
    int*? p = &v;
    int*? n = null;
    assert(p == v);
    assert(!(p == w));
    assert(!(n == v));
    assert(!(p != v));
    assert(p != w);
    assert(n != v);
}
