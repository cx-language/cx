// RUN: %delta -print-ir %s

interface Fooable {
    def foo()
}

struct F: Copyable, Fooable {
    def foo() { }
}

def main() {
    let f: F = undefined;
    bar(f);
}

def bar<T: Fooable>(t: T) {
    t.foo()
}
