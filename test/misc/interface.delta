// RUN: %delta -o=stdout %s

interface Fooable {
    func foo()
}

struct F { }

func F::foo() { }

func main() {
    const f: F = uninitialized;
    bar(f);
}

func bar<T: Fooable>(t: T) {
    t.foo()
}
