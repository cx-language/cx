// RUN: %cx run %s | %FileCheck -match-full-lines -strict-whitespace %s
// CHECK:1

struct Wrapper<T> {
    T value;
}

Wrapper<int> Wrapper(int a) {
    return Wrapper { value = a };
}

void main() {
    var b = Wrapper(1);
    println(b.value);
}
