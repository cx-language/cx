// RUN: %cx run %s | %FileCheck -match-full-lines -strict-whitespace %s
// CHECK:3
// CHECK-NEXT:3
// CHECK-NEXT:3
// CHECK-NEXT:1
// CHECK-NEXT:3
// CHECK-NEXT:1
// CHECK-NEXT:1

enum E {
    A,
    B,
}

void add(int a, int b,) {
    println(a + b);
}

void useAnonymousStruct((int a, float b,) t) {
    println(t.a);
}

void main() {
    add(1, 2,);
    add(a = 1, b = 2,);
    var array = [1, 2,];
    println(array[0] + array[1]);
    var single = [1,];
    println(single[0]);
    var anonymousStruct = (a = 1, b = 2,);
    println(anonymousStruct.a + anonymousStruct.b);
    println((1,));
    useAnonymousStruct((a = 1, b = 2.5,));
}
