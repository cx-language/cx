// RUN: check_exit_status 21 compile_and_run %s

struct Foo: Copyable {
    var value: int;

    init(value: int) {
        this.value = value;
    }

    def add(number: int): int {
        return this.value + number;
    }
}

def main(): int {
    var foo = Foo(42);
    return foo.add(-21);
}
