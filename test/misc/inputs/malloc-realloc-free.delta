extern func malloc(uint64 n) -> void*;
extern func realloc(void* p, uint64 n) -> void*;
extern func free(void* p);

func main() {
    var buffer = malloc(1);
    buffer = realloc(buffer, 4);
    free(buffer);
}
