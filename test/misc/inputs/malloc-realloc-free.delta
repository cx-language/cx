// compile_and_run_and_check_exit_status 0

extern func malloc(n: uint64) -> void*;
extern func realloc(p: void*, n: uint64) -> void*;
extern func free(p: void*);

func main() {
    var buffer = malloc(1);
    buffer = realloc(buffer, 4);
    free(buffer);
}
