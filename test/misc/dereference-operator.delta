// RUN: check_exit_status 42 compile_and_run %s

extern def calloc(count: uint64, size: uint64): mutable void*;

def main(): int {
    var buffer = cast<mutable int*>(calloc(1, 8));
    *buffer = 41;
    (*buffer)++;
    return *buffer;
}
