// RUN: check_exit_status 42 compile_and_run %s

extern mutable void* calloc(uint64 count, uint64 size);

int main() {
    var buffer = cast<mutable int*>(calloc(1, 8));
    *buffer = 41;
    (*buffer)++;
    return *buffer;
}
