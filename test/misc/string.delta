// RUN: check_exit_status 11 compile_and_run %s

def main(): uint {
    return "abc".size() + foo("quux");
}

def foo(x: StringRef): uint {
    var s = x;
    s = bar(x);
    return x.size() + bar(s).size();
}

def bar(z: StringRef): StringRef {
    return z;
}
