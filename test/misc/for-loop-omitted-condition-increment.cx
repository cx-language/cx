// RUN: %cx run %s | %FileCheck -match-full-lines -strict-whitespace %s
// CHECK:0
// CHECK-NEXT:1
// CHECK-NEXT:2
// CHECK-NEXT:0
// CHECK-NEXT:1
// CHECK-NEXT:2
// CHECK-NEXT:0
// CHECK-NEXT:1
// CHECK-NEXT:2

void main() {
    for (var i = 0;; i++) {
        if (i >= 3) {
            break;
        }
        println(i);
    }
    for (var j = 0; j < 3;) {
        println(j);
        j++;
    }
    for (var k = 0;;) {
        if (k >= 2) {
            break;
        }
        println(k);
        k++;
    }
    for (var s = 0;; s++) {
        if (s < 2) {
            continue;
        }
        if (s >= 3) {
            break;
        }
        println(s);
    }
}
