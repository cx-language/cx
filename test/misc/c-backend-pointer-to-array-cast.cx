// RUN: %cx --backend=c --print-c %s | %FileCheck %s

// A cast to pointer-to-array spells one pointer level, not two.

// CHECK: (uint8_t (*)[4])
// CHECK-NOT: (uint8_t (**)[4])

import "stdlib.h";

void main() {
    var buffer = cast<uint8[4]*>(malloc(4)!);
    buffer[0] = 70;
}
