// RUN: %cx run %s | %FileCheck -match-full-lines -strict-whitespace %s
// CHECK:42
// CHECK-NEXT:43

void bar(int*?* pp) {
    int*? p = *pp;
    println(p!);
}

void barPlain(int** pp) {
    int* p = *pp;
    println(*p);
}

void fooAddr(int*? p) {
    if (p == null) {
        return;
    }
    bar(&p);
}

void fooAddrPlain(int* p) {
    barPlain(&p);
}

void main() {
    int x = 42;
    fooAddr(&x);
    int y = 43;
    fooAddrPlain(&y);
}
