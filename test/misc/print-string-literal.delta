// RUN: %delta run %s | %FileCheck -match-full-lines -strict-whitespace %s
// CHECK:foo
// CHECK-NEXT:bar
// CHECK-NEXT:baz
// CHECK-NEXT:qux

import "stdio.h"

def foo(): char* { return "foo" }
def bar(): StringRef { return "bar" }

def main() {
    puts(foo())
    print(bar())
    puts("baz")
    print("qux")
}
