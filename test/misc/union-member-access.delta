// RUN: check_exit_status 42 %delta run %s

import "union-member-access.h"

def foo(u: U) -> int {
    return u.b
}

def main() -> int {
    var u: U = undefined
    u.a = 21
    return foo(u) + u.b
}
