// RUN: %not %cx test %s 2>&1 | %FileCheck %s

// CHECK-NOT: test-private-test-function.cx:{{[0-9]+}}:{{[0-9]+}}: {{(warning|error)}}:

private test void testPrivate() {
}

// CHECK: [[@LINE-3]]:19: error: test function 'testPrivate' cannot be private

void main() {
    testPrivate();
}

// CHECK-NOT: test-private-test-function.cx:{{[0-9]+}}:{{[0-9]+}}: {{(warning|error)}}:
