// RUN: cd %S && %cx run 2>&1 | %FileCheck -match-full-lines -strict-whitespace %s

// CHECK-NOT: warning
// CHECK:hello
// CHECK:nested
// CHECK-NOT: warning

void main() {
    println("hello");
    nestedHelper();
}
