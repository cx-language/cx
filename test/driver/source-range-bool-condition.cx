// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s
// CHECK-NOT: :{{[0-9]+}}:{{[0-9]+}}: {{(warning|error)}}:
int stringLength(string s) {
    return s.size();
}
void main() {
    while (stringLength("hi")) {
    }
}
// CHECK: [[@LINE-3]]:12: error: type 'int32' is not convertible to boolean
// CHECK-NEXT: while (stringLength("hi")) {
// CHECK-NEXT: ~~~~~~~~~~~~~~~~~~
// CHECK-NOT: :{{[0-9]+}}:{{[0-9]+}}: {{(warning|error)}}:
