// RUN: cd %S && %cx run 2>&1 | %FileCheck -match-full-lines -strict-whitespace %s

// CHECK-NOT: warning
// CHECK:hello from vendor
// CHECK-NOT: warning

import greet;

void main() {
    greet();
}
