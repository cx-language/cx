// RUN: %cx -print-llvm --release %s 2>&1 | %FileCheck %s

void callee() {}

void main() {
    callee();
}

// CHECK: define void @_CX1N4main6calleeE4void0_() #{{[0-9]+}} {
// CHECK: define i32 @main() #{{[0-9]+}} {
// CHECK-NOT: !DICompileUnit
// CHECK-NOT: !DISubprogram
// CHECK-NOT: !dbg
