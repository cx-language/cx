// RUN: %not %cx test %s 2>&1 | %FileCheck %s

// CHECK-NOT: test-generic-test-function.cx:{{[0-9]+}}:{{[0-9]+}}: {{(warning|error)}}:

test void testGeneric<T>() {
}

// CHECK: [[@LINE-3]]:11: error: generic test functions are not supported

void main() {
    testGeneric<int>();
}

// CHECK-NOT: test-generic-test-function.cx:{{[0-9]+}}:{{[0-9]+}}: {{(warning|error)}}:
