// RUN: %delta -print-ir %s | %FileCheck %s

struct S {}

interface I {
    func foo();
}

func ==<T: I>(a: T*, b: T*) {}

// CHECK: define void @"==[[SUFFIX:[\.0-9]+]]"({} %a, {} %b)
func ==(a: S, b: S) {}

func f(s: S) {
    // CHECK: call void @"==[[SUFFIX]]"({} %s, {} %s)
    _ = s == s;
}
