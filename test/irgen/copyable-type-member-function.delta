// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

struct Foo: Copyable {
    var baz: int;
    init() { this.baz = 42; }
    def bar() { }
    def qux(): int { return this.baz; }
}

def main() {
    var foo = Foo();
    foo.bar();
    var i = foo.qux();
}
