// RUN: check_matches_snapshot %s.ll %delta -print-ir %s -w

def f() {
    var index = 0;
    var x = g();
    X<int>().g();
}

def g(): X<int> {
    var x = X<int>();
    return x;
}

struct X<T> {
    init() {}

    def g() {
        var index = 0;
    }
}
