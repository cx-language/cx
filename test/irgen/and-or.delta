// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

extern def a(): bool;
extern def b(): bool;
extern def c(): bool;

def main() {
    var and = a() && b();
    var and2 = a() && b() && c();
    var or = a() || b();
    var or2 = a() || b() || c();
}
