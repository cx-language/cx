// RUN: check_matches_snapshot %s.ll %delta -print-ir %s -w

struct A {
    init() {}
    deinit() {}
}

def f(a: mutable A*) {
    a.deinit();
}

struct B {}

def g(b: mutable B*) {
    b.deinit();
}

def h(c: mutable char*) {
    c.deinit();
}
