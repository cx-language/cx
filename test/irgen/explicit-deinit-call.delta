// RUN: check_matches_snapshot %s.ll %delta -print-ir %s -Wno-unused

struct A {
    init() {}
    deinit() {}
}

void f(mutable A* a) {
    a.deinit();
}

struct B {}

void g(mutable B* b) {
    b.deinit();
}

void h(mutable char* c) {
    c.deinit();
}
