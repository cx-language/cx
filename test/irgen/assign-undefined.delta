// RUN: %delta -print-ir %s | %FileCheck %s

class A {
    init() {}
    deinit() {}
}

// CHECK: define void @f({}* %a)
func f(a: mutable A*) {
    // CHECK-NEXT: call void @A.deinit({}* %a)
    *a = undefined; // TODO: Replace this with a 'a.deinit()' call and put 'sema/assign-undefined-in-method' back on.
    // CHECK-NEXT: ret void
}
