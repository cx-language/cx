// RUN: check_matches_snapshot %s.ll %delta -print-ir %s -w

struct S {
    var i: int;
    init() {}
    def bar() {}
    def foo(bar: int) {
        var a = bar;
        bar();
    }
}

def main() {
    var s = S();
}
