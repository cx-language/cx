// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

struct Foo {
    var i: int;
}

def get(f: Foo*) {
    var m = f.i;
}

def main() {
    var f: Foo = undefined;
    var rf: Foo* = f;
    var n = rf.i;
    get(rf);
}
