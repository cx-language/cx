// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

struct X {
    var i: int;
    init() {
        i = 0;
    }
    deinit() {}
}

def foo(p: mutable X*, x: X) {
    p.init(x);
}

def main() {
    var x = X();
    var y = X();
    foo(x, y);
}
