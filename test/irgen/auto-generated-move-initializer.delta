// RUN: %delta -print-ir %s | %FileCheck %s

class X {
    let i: int;

    deinit() {}
}

// CHECK: define void @f(%X* %p, %X %x)
// CHECK-NEXT: store %X %x, %X* %p
// CHECK-NEXT: ret void
func f(p: mutable X*, x: X) {
    p.init(x);
}
