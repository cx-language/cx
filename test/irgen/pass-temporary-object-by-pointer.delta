// RUN: check_matches_snapshot %s.ll %delta -print-ir %s -w

struct A: Copyable {
    var i: int;
    init() {}
}

struct B {
    var i: int;
    init() {}
}

def f(a: A*) {}
def f(b: B*) {}
def f(i: int*) {}

def main() {
    var a: A = undefined;
    var b: B = undefined;
    f(A());
    f(B());
    f(0);
}
