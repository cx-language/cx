// RUN: check_matches_snapshot %s.ll %delta -print-ir %s -w

interface I {
    def i();
}

struct S: Copyable {
    var i: int;
    init() {}
    def i() {}
}

struct A<E> {
    init() {}

    def a(): E {
        return E();
    }
}

struct M<T: I> {
    var t: T;

    init() {}

    def f() {
        var a = A<T>();
        a.a().i();
    }
}

def main() {
    var m = M<S>();
    m.f();
}
