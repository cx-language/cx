// RUN: check_matches_snapshot %s.ll %delta -print-ir %s -Wno-unused

struct S<T>: Copyable {
    var f: T;
}

def ==<T>(a: S<T>, b: S<T>): bool {
    return true;
}

def f(c: S<int>, d: S<int>) {
    _ = c == d;
}

def f(c: S<bool>* , d: S<bool>* ) {
    _ = *c == *d;
}
