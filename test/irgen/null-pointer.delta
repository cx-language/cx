// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

def foo(): int*? { return null; }

def main() {
    var isNull = foo() == null;
    var isNonNull = foo() != null;
    var ptr = foo();
    var ptr2: bool*? = null;
}
