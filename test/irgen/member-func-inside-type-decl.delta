// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

struct A {
    var i: int
    init() { i = 0 }
    deinit() { }
    def foo() { }
    mutating def mutate() { }
}

def main() {
    var a = A()
    a.foo()
    a.mutate()
}
