// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

import "stdlib.h"

struct C<T>: Copyable {
    init() { }

    def f() {
        g()
    }

    def g() {
        var a = sizeof(T)
    }
}

def main() {
    var c = C<bool>()
    c.f()
}
