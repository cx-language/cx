// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

import "stdlib.h"

struct C<T>: Copyable {
    init() { }

    void f() {
        g()
    }

    void g() {
        var a = sizeof(T)
    }
}

void main() {
    var c = C<bool>()
    c.f()
}
