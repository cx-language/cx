// RUN: %delta -print-ir %s | %FileCheck %s

struct S: Copyable { }
struct C { }

def main() {
    // CHECK: %s = alloca {}
    var s: S = undefined;
    // CHECK-NEXT: %c = alloca {}
    var c: C = undefined;
    // CHECK-NEXT: ret
}
