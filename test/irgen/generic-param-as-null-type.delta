// RUN: %delta -print-ir %s | %FileCheck %s

struct S<T>: Copyable {
    var p: T*?

    init() {
        p = null
    }
}

func main() {
    // CHECK: call void @"S<int>.init"
    _ = S<int>()
    // CHECK: call void @"S<bool>.init"
    _ = S<bool>()
}
