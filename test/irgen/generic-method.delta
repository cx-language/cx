// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

struct X {
    init() {}

    def f<T>(t: () -> T): T {
        return t();
    }
}

def g(): int { return 0; }
def h(): bool { return false; }

def main() {
    var x = X();
    x.f(g);
    x.f(h);
}
