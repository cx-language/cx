// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

struct X {
    init() {}
    deinit() {}
}

struct Y<T> {
    init() {}

    def f(): int {
        return 1;
    }
}

def main() {
    var a = X();
    Y<bool>().f();
}
