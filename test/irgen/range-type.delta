// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

def foo(r: Range<int>): int {
    var sum = 0;
    for (var i in r) {
        sum += i;
    }
    return sum;
}

def main(): int {
    return foo(0..5);
}
