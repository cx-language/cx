// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

extern def f(): mutable void*?;

def main() {
    var byte: mutable uint8* = cast<mutable uint8*?>(f())!;
    var ptr: mutable uint8[1]*? = cast<mutable uint8[1]*?>(f());
    ptr![0] = 1;
}
