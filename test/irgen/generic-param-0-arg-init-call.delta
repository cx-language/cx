// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

struct C {
    var i: int;
    init() { i = 0; }
}

struct A<T> {
    init() {
        var t = T();
    }
}

def main() {
    var a = A<C>();
}
