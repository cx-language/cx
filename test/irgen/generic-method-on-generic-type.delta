// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

struct X<T> {
    init() {}
    mutating void f<U>(T(U) u) {}
}

int g(bool i) { return int(i); }

void main() {
    var x = X<int>();
    x.f(g);
}
