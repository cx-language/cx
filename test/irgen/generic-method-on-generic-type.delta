// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

struct X<T> {
    init() {}
    mutating def f<U>(u: (U) -> T) {}
}

def g(i: bool): int { return int(i); }

def main() {
    var x = X<int>();
    x.f(g);
}
