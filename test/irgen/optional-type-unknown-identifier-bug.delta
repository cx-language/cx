// RUN: %delta -print-ir %s

struct X<T> {
    var y: Y<T>*?;

    init() {
        y = null;
    }

    deinit() {
        var a = y!.a;
    }
}

struct Y<T> {
    var a: T;

    def f() {}
}

def main() {
    var x = X<int>();
}
