// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

def main() {
    var x = X(4, 2);
}

struct X {
    init(a: int, b: int) {
        init(a);
    }

    init(a: int) {
        init();
    }

    init() {}
}
