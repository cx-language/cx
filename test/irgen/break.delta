// RUN: %delta -print-ir %s | %FileCheck %s

func main() {
    while (true) {
        if (true) {
            // CHECK:      then:
            // CHECK-NEXT:   br label %endwhile
            break;
            // CHECK-NOT: %a
            // CHECK-NOT: alloca
            // CHECK-NOT: 1
            const a = 1;
        }
        // CHECK:      endif:
        // CHECK-NEXT:   br label %endwhile
        break;
    }
    switch (1) {
        default:
            // CHECK:      default:
            // CHECK-NEXT:   br label %endswitch
            break;
            // CHECK-NOT: %b
            // CHECK-NOT: alloca
            // CHECK-NOT: 1
            const b = 1;
    }
}
