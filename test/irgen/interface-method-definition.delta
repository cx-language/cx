// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

interface A {
    int f();
    int g() { return f() * 2; }
}

struct B: A {
    init() {}
    int f() { return 21; }
}

void main() {
    var b = B();
    var x = b.g();
}
