// RUN: %delta -print-ir %s -I%p/inputs | %FileCheck %s

import imported_generic_type_init

func main() {
    // CHECK: %a = alloca %"A<int>"
    // CHECK: call void @"A<int>.init"(%"A<int>"* %a)
    var a = A<int>()
}
