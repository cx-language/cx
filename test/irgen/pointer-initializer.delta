// RUN: check_matches_snapshot %s.ll %delta -print-ir %s -Wno-unused

// TODO: Should work even if 'p' and 'x' point to mutable chars.
def foo(p: char**, x: char*) {
    var pp: char** = undefined;
    (&pp).init(p);
    pp.init(x);
    (*pp).init('x');
}
