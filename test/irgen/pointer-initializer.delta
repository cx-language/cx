// RUN: check_matches_snapshot %s.ll %delta -print-ir %s -Wno-unused

def foo(p: char mutable**, x: char*) {
    var pp: char mutable** = undefined;
    (&pp).init(p);
    pp.init(x);
    (*pp).init('x');
}
