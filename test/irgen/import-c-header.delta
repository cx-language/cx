// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

import "stdio.h";
import "foo.h";

def main() {
    puts("foo bar");
    var f: Foo = undefined;
    f.bar = MAGIC_NUMBER + ANSWER;
    f.baz = "foo";
    var bar = getBar(f);
}
