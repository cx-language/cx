// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

def main() {
    var three = [0, 42, 0];
    foo(three);
    bar(three);
    var b = three.size();
    var ref: int[]* = three;
    var bb = ref.size();
}

def foo(ints: int[]*) {
    var a = ints[1];
    var b = ints.size();
    var c: int[]* = ints;
}

def bar(ints: int[3]*) {
    var b = ints.size();
    var ref: int[]* = ints;
}
