// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

struct X<T> {
    def f<U>(u: U): X<U> {
        var x: X<U> = undefined;
        return x;
    }

    def g() {}
}

def main(x: X<int>) {
    var a = x.f(false);
    a.g();
}
