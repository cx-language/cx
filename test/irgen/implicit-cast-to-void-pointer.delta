// RUN: check_matches_snapshot %s.ll %delta -print-ir %s

void main() {
    int* p = undefined;
    mutable int* mp = undefined;
    void* v = p;
    bar(p);
    bar(v);
    var b = cast<int8*>(v);
    v = b;
    baz(mp);
    var i = 0;
    baz(&i);
}

void bar(void* p) {}

void baz(void*? p) {}
