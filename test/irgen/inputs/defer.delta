extern func foo();
extern func bar();

func main() {
    defer foo();
    if (false) { defer bar(); return; }
    if (false) { defer bar(); }
    while (false) { defer foo(); bar(); }
    defer bar();
    if (true) { return; }
    foo();
}

func shouldBeEmpty() { }
