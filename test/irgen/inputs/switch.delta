func foo(i: int) {
    switch (i) {
        case 0:
            break;
        case -1:
            foo(i);
            foo(i);
        case 42:
            break;
    }
    switch (i) {
        case 0:
            break;
        default:
            foo(i);
    }
}
