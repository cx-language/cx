func main() {
    var three = [0, 42, 0];
    foo(&three);
    bar(&three);
    const b = three.count;
    int[]& ref = &three;
    const bb = ref.count;
}

func foo(int[]& ints) {
    const a = ints[1];
    const b = ints.count;
    int[]* c = ints;
}

func bar(int[3]& ints) {
    const b = ints.count;
    int[]* ref = ints;
}
