func main() {
    var three = [0, 42, 0];
    foo(&three);
    bar(&three);
    const b = three.count;
    const ref: int[]& = &three;
    const bb = ref.count;
}

func foo(ints: int[]&) {
    const a = ints[1];
    const b = ints.count;
    const c: int[]* = ints;
}

func bar(ints: int[3]&) {
    const b = ints.count;
    const ref: int[]* = ints;
}
