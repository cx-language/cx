class Foo {
    const i: int;
    deinit() { }
}

struct Bar {
    const i: int;
    deinit() { }
}

func main() {
    const f: Foo = uninitialized;
    if (false) {
        const f2: Foo = uninitialized;
    }
    const b: Bar = uninitialized;
    while (false) {
        const b2: Bar = uninitialized;
        return;
    }
    var i = 1;
}
