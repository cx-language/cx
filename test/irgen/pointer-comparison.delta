// RUN: check_matches_snapshot %s.ll %delta -print-ir %s -w

struct X { var i: int; }
struct Y { var i: int; }

def <(a: X*, b: X*): bool {
    return true;
}

interface Z {
    def z();
}

def < <T: Z>(a: T*, b: T*): bool {
    return true;
}

def fx(a: X*, b: X*) {
    _ = a < b;
    _ = *a < *b;
}

def fy(a: Y*, b: Y*) {
    _ = addressof(a) < addressof(b);
}
