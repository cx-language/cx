// RUN: check_matches_snapshot %s.ll %delta -print-ir %s -Wno-unused

struct S<T> {
    var t: T;

    mutating def [](i: int): mutable T* {
        return t;
    }
}

def f(s: mutable S<int>*) {
    s[0]++;
}
