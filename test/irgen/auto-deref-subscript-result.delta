// RUN: check_matches_snapshot %s.ll %delta -print-ir %s -Wno-unused

struct S<T> {
    T t;

    mutating mutable T* operator[](int i) {
        return t;
    }
}

void f(mutable S<int>* s) {
    s[0]++;
}
