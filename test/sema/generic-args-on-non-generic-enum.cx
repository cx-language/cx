// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s


enum E {
    A(int a),
}

void main() {
    // CHECK: [[@LINE+1]]:15: error: too many generic arguments to 'E', expected 0
    var x = E.A<int>(a = 1);
}

