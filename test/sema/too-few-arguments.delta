// RUN: not %delta -typecheck %s | %FileCheck %s

def sum(a: int, b: int): int { return a + b; }

// CHECK: [[@LINE+1]]:24: error: too few arguments to 'sum', expected 2
def main() { var foo = sum(1); }
