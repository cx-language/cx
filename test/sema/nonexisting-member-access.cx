// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

struct Foo {
    int qux;
}

void main() {
    var foo = Foo(1);
    // CHECK: [[@LINE+1]]:19: error: no member named 'bar' in 'Foo'
    var bar = foo.bar;
}
