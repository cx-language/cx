// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

extern int[5]* foo();

void main() {
    var p = foo();
    // CHECK: [[@LINE+1]]:15: error: illegal index type 'bool', expected 'int32'
    var e = p[false];
}
