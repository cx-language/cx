// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

void main() {
    // CHECK: [[@LINE+1]]:13: error: illegal cast from 'bool' to 'int**'
    var i = cast<int**>(false);
    var cp = cast<const int*>(0);
    // CHECK: [[@LINE+1]]:13: error: illegal cast from 'const int*' to 'uint*'
    var p = cast<uint*>(cp);
}
