// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

int foo(int a) { return a; }

void main() {
    var f = false;
    // CHECK: [[@LINE+1]]:17: error: invalid argument #1 type 'bool' to 'foo', expected 'int32'
    var z = foo(f);
}
