// RUN: %cx -typecheck %s %p/a.cx 2>&1 | %FileCheck %s
// RUN: %cx -typecheck %s %p/a.cx 2>&1 | %FileCheck %p/a.cx
// RUN: %cx -typecheck %s %p/a.cx 2>&1 | %FileCheck %p/a.cx -check-prefix=CHECK-PRIVATE-IN-PUBLIC-DECL

void main() {
    // CHECK: [[@LINE+1]]:5: warning: 'A' is private
    A a = undefined;

    // CHECK: [[@LINE+1]]:9: warning: 'A' is private
    a = A();
}

// CHECK: [[@LINE+1]]:8: warning: 'A' is private
void f(A a) {}

// CHECK: [[@LINE+1]]:8: warning: 'A' is private
void g(A*? a) {}

// CHECK: [[@LINE+1]]:1: warning: 'A' is private
A*? h() {
    return null;
}

struct B {
    // CHECK: [[@LINE+1]]:5: warning: 'A' is private
    A a;
}
