// RUN: not %delta -typecheck %s | %FileCheck %s

def foo<T, U>(a: T, b: U) -> T { return a; }

// CHECK: [[@LINE+1]]:30: error: too few generic arguments to 'foo', expected 2
def main() { var f = foo<int>(1, 2); }
