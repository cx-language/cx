// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s


enum Outcome<T> {
    Ok(T value),
    Err(int code),
}

void main() {
    // CHECK: [[@LINE+1]]:21: error: enum 'Outcome' has no case named 'Bogus'
    var x = Outcome.Bogus;
}

