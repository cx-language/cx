// RUN: %not %cx -typecheck %s -Wno-unused 2>&1 | %FileCheck %s
// CHECK-NOT: main.cx:{{[0-9]+}}:{{[0-9]+}}: {{(warning|error)}}:

import inputs

void main() {
    // CHECK: [[@LINE+1]]:16: error: cannot assign 'int' to 'Secret'
    Secret s = 42;
    // The same conversion applies within its own module.
    var s2 = make();
}

// CHECK-NOT: main.cx:{{[0-9]+}}:{{[0-9]+}}: {{(warning|error)}}:
