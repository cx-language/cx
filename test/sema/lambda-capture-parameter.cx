// RUN: %cx -typecheck %s 2>&1 | %FileCheck %s --allow-empty
// CHECK-NOT: warning
// CHECK-NOT: error

void foo(int a) {
    var d = 3;
    var b = (int c) => c + a + d;
    _ = b(1);
}

void main() {
    foo(2);
}
