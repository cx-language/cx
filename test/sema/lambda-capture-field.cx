// RUN: %cx -typecheck %s | %FileCheck %s --allow-empty
// CHECK-NOT: warning
// CHECK-NOT: error

struct S {
    int d;

    int foo(int c) {
        var b = (int x) => x + c + d;
        return b(1);
    }
}

void main() {
    var s = S(d = 5);
    _ = s.foo(1);
}
