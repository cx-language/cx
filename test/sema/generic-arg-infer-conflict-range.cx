// RUN: %not %cx -typecheck -Wno-unused %s 2>&1 | %FileCheck %s

void f(uint a) {
    // CHECK: [[@LINE+1]]:17: error: no matching operator '..' with arguments 'float' and 'uint'
    for i in 0.0..a {}
}
