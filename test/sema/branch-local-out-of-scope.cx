// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

// CHECK-NOT: error
// CHECK-NOT: warning

void main() {
    var c = true;
    if c {
        var y = 10;
    }
    // CHECK: [[@LINE+1]]:13: error: unknown identifier 'y'
    println(y);
}

// CHECK-NOT: error
// CHECK-NOT: warning
