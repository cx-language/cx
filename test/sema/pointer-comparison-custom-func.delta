// RUN: not %delta -typecheck %s | %FileCheck %s

class X {}

func fx(a: X*, b: X*) {
    // CHECK: [[@LINE+1]]:11: error: no matching function for call to '<' with argument list of type '(X*, X*)'
    _ = a < b;
}
