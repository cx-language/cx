// RUN: not %delta -typecheck %s | %FileCheck %s

class X {}

func fx(a: X*, b: X*) {
    // CHECK: [[@LINE+1]]:11: error: type 'X' doesn't implement interface 'Comparable' because it doesn't have member function 'compare'
    _ = a < b;
}
