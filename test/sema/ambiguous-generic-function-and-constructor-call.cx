// RUN: %cx -typecheck %s

struct Wrapper<T> {
    T value;
}

Wrapper<T> Wrapper<T>(T a) {
    return Wrapper { value = a };
}

void main() {
    // Parenthesized calls resolve to the function, brace calls to the constructor.
    var b = Wrapper<int>(1);
    var c = Wrapper<int> { value = 2 };
}
