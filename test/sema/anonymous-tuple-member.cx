// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

void main() {
    // CHECK: [[@LINE+1]]:14: error: anonymous tuple members are not supported yet; name each element (e.g. `(x = 1, y = 2)`)
    var t = (1, 2);
}
