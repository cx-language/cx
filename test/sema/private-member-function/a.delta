// RUN: true

// CHECK-NOT: a.delta:{{[0-9]+}}:{{[0-9]+}}: {{(warning|error)}}: '{{(f|A\.f)}}' is private

struct A {
    init() {}
    private def f() {}
    def g() {
        this.f();
        f();
    }
}

def g() {
    let a = A();
    a.g();
}
