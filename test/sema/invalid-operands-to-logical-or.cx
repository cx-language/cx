// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

void main() {
    var foo = false;
    var bar = 0;
    // CHECK: [[@LINE+1]]:13: error: invalid operands 'bool' and 'int32' to '||'
    if (foo || bar) { }
}
