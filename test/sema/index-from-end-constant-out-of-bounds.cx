// RUN: %not %cx -typecheck %s -Werror 2>&1 | %FileCheck %s
void main() {
    int[3] numbers = [10, 20, 30];
    // CHECK: [[@LINE+1]]:{{[0-9]+}}: error: accessing array out-of-bounds with index -1, array size is 3
    println(numbers[-4]);
}
