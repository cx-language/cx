// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

// CHECK-NOT: this-assignment.cx:{{[0-9]+}}:{{[0-9]+}}: {{(warning|error)}}:

struct Foo {
    void replace(Foo* other) {
        // CHECK: [[@LINE+1]]:9: error: cannot assign to 'this'
        this = other;
    }
}
