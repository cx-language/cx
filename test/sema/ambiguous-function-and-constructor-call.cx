// RUN: %cx -typecheck %s

struct Rectangle {
    int x;
}

Rectangle Rectangle(int a) {
    return Rectangle { a };
}

void main() {
    // Parenthesized calls resolve to the function, brace calls to the constructor.
    var r = Rectangle(1);
    var s = Rectangle { 2 };
}
