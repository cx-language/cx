// RUN: %delta -typecheck %s

struct A {
    init() {}
}

def f<T>() {
    let t = T();
    let a = t;
}

def g<T>(t: T) {
    let a = t;
}

def main() {
    f<A>();
    f<A>();
    g(A());
    g(A());
}
