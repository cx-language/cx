// RUN: %delta -typecheck %s

struct A {
    init() {}
}

def f<T>() {
    var t = T();
    var a = t;
}

def g<T>(t: T) {
    var a = t;
}

def main() {
    f<A>();
    f<A>();
    g(A());
    g(A());
}
