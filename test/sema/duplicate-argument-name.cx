// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s


struct S: Copyable {
    int a;
    int b;
}

void main() {
    // CHECK: [[@LINE+1]]:22: error: duplicate argument for parameter 'a'
    var s = S(a = 1, a = 2);
}

