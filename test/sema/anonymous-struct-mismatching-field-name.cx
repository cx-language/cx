// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

(int a, int b) f() {
    // CHECK: [[@LINE+1]]:5: error: mismatching return type '(int32 a, int32 c)', expected '(int32 a, int32 b)'
    return (a = 0, c = 0);
}
