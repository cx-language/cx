// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

(int? a, int b) make() {
    // CHECK: [[@LINE+1]]:5: error: mismatching return type '(int a, int b, int c)', expected '(int? a, int b)'
    return (a = 1, b = 2, c = 3);
}


void main() {
    make();
}
