// RUN: not %delta -typecheck %s | %FileCheck %s

struct C<T> {
    init() { }

    deinit() {
        // CHECK: [[@LINE+1]]:17: error: unknown identifier 'b'
        var a = b
    }
}

def main() {
    var c = C<int>()
}
