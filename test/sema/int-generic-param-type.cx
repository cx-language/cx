// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

// CHECK-NOT: int32-generic-param-type.cx:{{[0-9]+}}:{{[0-9]+}}: {{(warning|error)}}:

// CHECK: [[@LINE+1]]:13: error: generic parameters must be types or integers
struct B<T, string S> {
    T value;
}

// CHECK-NOT: int32-generic-param-type.cx:{{[0-9]+}}:{{[0-9]+}}: {{(warning|error)}}:
