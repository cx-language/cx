// RUN: not %delta -typecheck %s | %FileCheck %s

// TODO: Make the error message location point to the type rather than the function name.
// CHECK: [[@LINE+1]]:6: error: too many generic arguments to 'int', expected 0
func f() -> int<bool> {
    return 42;
}
