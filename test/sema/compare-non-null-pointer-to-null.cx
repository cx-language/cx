// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

void main() {
    var i = 42;
    var p = &i;
    // CHECK: [[@LINE+1]]:11: error: invalid operands 'int32*' and 'null' to '==' (non-optional type 'int32*' cannot be null)
    if (p == null) { }
}
