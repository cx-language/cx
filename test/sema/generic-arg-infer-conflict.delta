// RUN: not %delta -typecheck %s | %FileCheck %s

def foo<T>(a: T, b: T) { }

// CHECK: [[@LINE+1]]:17: error: couldn't infer generic parameter 'T' of 'foo' because of conflicting argument types 'int' and 'bool'
def main() { foo(1, false) }
