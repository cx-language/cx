// RUN: %cx -typecheck --Werror %s | %FileCheck %s -allow-empty
// RUN: %cx -typecheck %s | %FileCheck %s -allow-empty

// CHECK-NOT: warning
// CHECK-NOT: error

import mod;

void main() {
    helper();
}
