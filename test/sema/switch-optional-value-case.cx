// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s
// CHECK-NOT: switch-optional-value-case.cx:{{[0-9]+}}:{{[0-9]+}}: {{(warning|error)}}:

void main() {
    int? o = 1;
    switch (o) {
        // CHECK: [[@LINE+1]]:14: error: case value type 'int32' doesn't match switch condition type 'int32?'
        case 1: break;
    }
}

// CHECK-NOT: switch-optional-value-case.cx:{{[0-9]+}}:{{[0-9]+}}: {{(warning|error)}}:
