// RUN: not %delta -typecheck %s | %FileCheck %s

struct S: Copyable {
    mutable S*? a;
}

void foo(mutable S* p) {
    if (p.a != null) {
        // CHECK: [[@LINE+1]]:12: error: cannot unwrap non-optional type 'mutable S*'
        p.a!.a = p;
    }
}
