// RUN: %not %cx -typecheck %s -Wno-unused 2>&1 | %FileCheck %s

void foo(int*? p) {
    if (p == null) {
        return;
    }
    defer println("foo");
    // CHECK: [[@LINE+1]]:16: error: type 'int*' is not optional and has no 'unwrap' method
    var a = *(p!);
}
