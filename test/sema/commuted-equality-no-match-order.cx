// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s


struct A {
    int x;
}

struct B {
    int y;
}

void main() {
    var a = A(x = 1);
    var b = B(y = 2);
    // CHECK: [[@LINE+1]]:15: error: no matching operator '==' with arguments 'A' and 'B'
    println(a == b);
}

