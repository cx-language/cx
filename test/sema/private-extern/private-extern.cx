// RUN: %cx -typecheck %s %p/a.cx 2>&1 | %FileCheck %s
// RUN: %cx -typecheck %s %p/a.cx 2>&1 | %FileCheck %p/a.cx

void main() {
    // CHECK: private-extern.cx:[[@LINE+1]]:5: warning: 'f' is private
    f();
}
