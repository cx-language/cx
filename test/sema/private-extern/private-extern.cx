// RUN: %cx -typecheck %s 2>&1 | %FileCheck %s
// RUN: %cx -typecheck %s 2>&1 | %FileCheck %p/mod/a.cx
import mod;

void main() {
    // CHECK: private-extern.cx:[[@LINE+1]]:5: warning: 'f' is private
    f();
}
