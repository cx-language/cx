// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

// CHECK-NOT: error
// CHECK-NOT: warning

void main() {
    var a = [1,2,3]
    var s = sizeof(a)
    // CHECK: [[@LINE+1]]:20: error: 'a' is not a type
    var e = sizeof(a[0])
}

// CHECK-NOT: error
// CHECK-NOT: warning
