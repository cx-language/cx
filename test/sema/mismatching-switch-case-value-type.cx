// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

void main() {
    switch (1) {
        // CHECK: [[@LINE+1]]:14: error: case value type 'bool' doesn't match switch condition type 'int32'
        case false: break;
    }
}
