// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

import "inputs/mixed-c-duplicate-a.h";
import "inputs/mixed-c-duplicate-b.h";

void main() {
    // CHECK: [[@LINE+1]]:13: error: ambiguous reference to 'widget'
    var w = widget;
}
