// RUN: %cx -typecheck --check-all %s 2>&1 | %FileCheck --allow-empty %s
// The standard library is fully typechecked under --check-all, so an empty
// program validates all of std. The implicit no-unexpected-diagnostics guard
// in %FileCheck covers the std files too, so no explicit checks are needed.

void main() {
}
