// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s


struct Foo {
    int i;
    void bar() {
        // CHECK: [[@LINE+1]]:18: error: cannot assign 'Foo&' to 'Foo*' (use '&' to take the address explicitly)
        Foo* f = this;
    }
}
