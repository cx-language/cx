// RUN: %cx -typecheck %s 2>&1 | %FileCheck %s

void main() {
    var content = readFile("definitely-not-here-12345").unwrap();
    // CHECK: [[@LINE+1]]:27: warning: unwrapping non-optional type 'StringBuf' has no effect
    println(string(content!));
}
