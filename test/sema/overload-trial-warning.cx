// RUN: %cx -typecheck -Wno-unused %s 2>&1 | %FileCheck %s

void take(int x) {}
void take(string s) {}

void main() {
    int[3] arr = [1, 2, 3];
    // CHECK: [[@LINE+1]]:14: warning: accessing array out-of-bounds with index 10, array size is 3
    take(arr[10]);
}
