// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s


enum Outcome<T> {
    Ok(T value),
    Err(int code),
}

void main() {
    // CHECK: [[@LINE+1]]:33: error: invalid argument #1 type 'string' to 'Ok', expected 'int'
    Outcome<int> r = Outcome.Ok(value = "s");
}

