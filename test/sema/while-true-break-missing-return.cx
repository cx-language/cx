// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

// CHECK: [[@LINE+1]]:5: error: 'f' is missing a return statement
int f(int n) {
    while true {
        if n > 0 {
            break;
        }
        return 0;
    }
}

void main() {
    _ = f(1);
}
