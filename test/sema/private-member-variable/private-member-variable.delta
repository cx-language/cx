// RUN: %check_errors %delta -typecheck %s %p/a.delta
// RUN: %check_errors %delta -typecheck %s %p/a.delta

void main() {
    var a = A();
    // CHECK: [[@LINE+1]]:7: warning: 'a' is private
    a.a++;
}
