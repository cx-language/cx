// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

void main() {
    // CHECK: [[@LINE+1]]:14: error: unnamed anonymous struct members are not supported yet; name each field (e.g. `(x = 1, y = 2)`)
    var t = (1, 2);
}
