// RUN: %cx -typecheck %s 2>&1 | %FileCheck -allow-empty %s

void main() {
    // Negative constants shift arithmetically, so this fits in int8.
    int8 y = -16 >> 2;
    int8 z = 16 >> 2;
    assert(y == -4);
    assert(z == 4);
}
