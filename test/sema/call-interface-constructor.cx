// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

interface Printable {
    void print(StringBuffer* stream);
}

void main() {
    // CHECK: [[@LINE+1]]:5: error: cannot construct interface 'Printable'
    Printable();
}
