// RUN: true

// CHECK-NOT: a.cx:{{[0-9]+}}:{{[0-9]+}}: {{(warning|error)}}: '{{(f|A\.f)}}' is private

enum A {
    X,

    private void f() {}

    void g() {
        this.f();
        f();
    }
}
