// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

bool foo<T>(T t) {
    // CHECK: [[@LINE+1]]:14: error: invalid operands 'bool' and 'int32' to '<'
    return t < 3;
}

void main() {
    _ = foo(false);
}
