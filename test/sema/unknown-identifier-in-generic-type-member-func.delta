// RUN: not %delta -typecheck %s | %FileCheck %s

struct S<T>: Copyable {
    def foo() {
        // CHECK: [[@LINE+1]]:9: error: unknown identifier 'x'
        x++
    }
}

def bar(s: S<int>) {
    s.foo()
}
