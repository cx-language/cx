// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

// CHECK-NOT: nullable-borrow-return.cx:{{[0-9]+}}:{{[0-9]+}}: {{(warning|error)}}:

// CHECK: [[@LINE+1]]:1: error: reference type 'int&?' may only appear as a function parameter type
int&? returnsNullableBorrow() {
    var x = 1;
    return x;
}

void main() {
}

// CHECK: 6:7: warning: unused declaration 'returnsNullableBorrow'
// CHECK-NOT: nullable-borrow-return.cx:{{[0-9]+}}:{{[0-9]+}}: {{(warning|error)}}:
