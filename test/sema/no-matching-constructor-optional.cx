// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s


void main() {
    // Optional is an enum; construct values with Optional.Some/Optional.None instead.
    // CHECK: [[@LINE+1]]:14: error: no matching constructor 'Optional(int)'
    int? x = Optional(2);
}

