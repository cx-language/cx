// check_error 6:5 "cannot subscript 'mutable uint8*', expected array or reference-to-array"
extern func malloc(uint64 n) -> mutable void*;

func main() {
    mutable uint8* p = cast<mutable uint8*>(malloc(8));
    p[0] = 64;
}
