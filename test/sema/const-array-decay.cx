// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s

void main() {
    const int[2] a = [1, 2];
    // CHECK: [[@LINE+1]]:18: error: cannot assign 'const int[*]' to 'int[*]'
    int[*] p = a.data();
}
