// RUN: %not %cx -typecheck -Wno-unused %s 2>&1 | %FileCheck %s

struct C<T> {
    ~C() {
        // CHECK: [[@LINE+1]]:17: error: unknown identifier 'b'
        var a = b
    }
}

void main() {
    var c = C<int>()
}
