// RUN: %delta -typecheck %s

struct S<T>: Copyable {
    init() { }

    def f(t: T) {
        let p: mutable T*? = null;
        *(p!) = t;
    }
}

def main() {
    let s = S<int>();
    s.f(1);
}
