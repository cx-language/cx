// RUN: %delta -typecheck %s

struct S<T>: Copyable {
    init() { }

    def f(t: T) {
        var p: mutable T*? = null;
        *(p!) = t;
    }
}

def main() {
    var s = S<int>();
    s.f(1);
}
