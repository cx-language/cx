// RUN: %not %cx -typecheck %s 2>&1 | %FileCheck %s


enum Outcome<T> {
    Ok(T value),
    Err(int code),
}

void main() {
    // CHECK: [[@LINE+1]]:13: error: unknown identifier 'Ok'
    var a = Ok(1);
    // CHECK: [[@LINE+1]]:13: error: unknown identifier 'None'
    var b = None;
}

