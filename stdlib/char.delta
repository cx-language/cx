struct char: Copyable, Comparable, Hashable {
    def compare(other: char*): Ordering {
        return int(this).compare(int(*other));
    }

    def hash(): uint64 {
        return uint64(this);
    }
}
