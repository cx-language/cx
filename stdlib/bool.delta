struct bool: Copyable, Comparable, Hashable {
    def compare(other: bool*): Ordering {
        return int(this).compare(int(*other));
    }

    def hash(): uint64 {
        return uint64(this);
    }
}
