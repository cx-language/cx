// Naive recursive Fibonacci. Workload: call overhead + integer arithmetic.
// Output must be stable.
int fib(int n) {
    if n < 2 return n;
    return fib(n - 1) + fib(n - 2);
}

void main() {
    println(fib(40));
}
